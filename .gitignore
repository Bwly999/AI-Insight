# dependencies
node_modules
.pnpm-store

# build tooling (开发期绕路装 esbuild 的独立目录，见 packages/cli/scripts/bundle.mjs)
.build-tools/

# build outputs
dist
build
.vite
*.tsbuildinfo

# turbo
.turbo

# env (keep secrets local; ship a .env.example template)
.env
.env.local
.env.*.local

# data / db
data
*.db
*.db-journal
*.db-wal
*.db-shm

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os
.DS_Store
Thumbs.db

# editor
.idea
.vscode/*
!.vscode/settings.json

# pi agent runtime home (local)
.pi-home
.playwright-cli/

# aiinsight CLI local config (明文 API key，见 docs/design/skills-融合-cli-通用数据源能力.md §6)
# 默认在 ~/.aiinsight/，但若用户用 AIINSIGHT_CONFIG 指向仓库内则需此规则保护
aiinsight.config.json
.aiinsight/
