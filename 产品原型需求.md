当前项目是一个空的项目，里面只有一些参考项目(在ref目录下，newsnow是各个平台的热点爬取项目、Signex是一套包含数据获取+分析的skill、union-search-skill是一个聚合搜索skill)，我要实现的是一个AI For洞察的全栈项目，部署在公司内网用于洞察行业的咨询
## 前端介绍
前端分为两个端，用户端和管理端

### 用户端
用户端的前端的主体是一个搜索网页，布局类似openai的chatgpt网页, 在前端完成AI For洞察的全流程，通过对话发起洞察，然后AI根据意图来调用工具，搜索各种数据源并提取数据，最后汇总为对应的报告, 支持普通的markdown报告(可以导出为pdf)和html网页版本的报告。
洞察可以选择数据源的时间，如1天内、1周内、1个月内、半年内、1年内、不限制等
然后支持定时任务，可以把一次洞察prompt配置为定时任务，到时间自动完成全流程的洞察并整理好报告

### 管理端
管理员使用的后端，可以查看每个洞察任务的运行情况、定时任务情况、设置后端运行的代理、数据源管理(包含数据源的启停、标签的设置、新增/删除数据源(只限于RSS订阅))等

## 后端介绍
后端提供Agent的能力、搜索的能力、调度运行的能力等

### Agent
Agent使用Pi Agent Sdk, 这是一个typescript的agent类库，具体使用方法使用find docs来查询，用它作为agent能力的基座，用来实现类似codex、claude code一样的agent能力

### 数据源
数据源有3种，分别是搜索、RSS、爬虫，对于详细的数据源支持配置标签，如通用、新闻、学术等等，方便Agent针对性调用
#### 搜索
采用多种搜索引擎聚合搜索然后去重，搜索使用DuckDuckGo、Exa、Firecrawl

#### RSS
接入RSS HUB和一些公开的RSS订阅，来获取数据

#### 爬虫
具体follow ref下的newsnow项目，把他爬取各个平台热点的能力接入

### 代理功能
因为是内网环境，后端调用数据源中的搜索+RSS功能时要支持使用代理，从而保证外网能正常访问

### 用户登录
api请求的时候在header中带上token，token在前端的获取和在服务器的解析预留好对应接口，用于我在内网中完成具体的实现，dev模式下可以实现一版最简单的来保障基础的功能调试

## 技术栈
使用typescript+nextjs完成全栈的开发，css库使用tailwindcss v4